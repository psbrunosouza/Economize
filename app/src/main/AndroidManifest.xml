<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dev.morganizze">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activity.DespesasActivity" android:theme="@style/despesaTema"></activity>
        <activity android:name=".Activity.ReceitaActivity" android:theme="@style/receitaTema"/>
        <activity
            android:name=".Activity.MainActivity"
            android:theme="@style/introTema" />
        <activity
            android:name=".Activity.LoginActivity"
            android:theme="@style/introTema" />
        <activity
            android:name=".Activity.CadastrarActivity"
            android:theme="@style/introTema" />
        <activity
            android:name=".Activity.IntroMainActivity"
            android:theme="@style/introTema">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>